post_install() {
  # Replacing the original /etc/skel/.bashrc file with the modified Biglinux file
  if [[ -e "/etc/skel/.dot.bashrc" ]]; then
    mv -f /etc/skel/.dot.bashrc /etc/skel/.bashrc
  fi
}

post_upgrade() {
  post_install
}

